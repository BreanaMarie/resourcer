<section>
  <div class="container">
    <div class="row">
      <div class="col-xs-4 searchnav">
        <h4>Search by Keyword</h4>
        <%= form_tag profiles_path, :method => 'get', :id => "profiles_search" do %>
          <%= hidden_field_tag :direction, params[:direction] %>
          <%= hidden_field_tag :sort, params[:sort] %>

          <p>
            <%= text_field_tag :search, params[:search], placeholder: "Search Profiles", class: "form-group" %>
          </p>
          <%= submit_tag "Search", :name => nil, class: "btn btn-primary" %>

        <% end %>
      </div>


      <div class="col-xs-8">
        <div id="profiles"><%= render 'profiles' %></div>
            <div class="table thumbnail" width:"100%">
              <% if current_user.profile.seeker == 'candidate' %>
                <% for profile in @profiles.where(seeker: "sourcer") %>

                  <div class="result">
                    <div class="row">

                    <div class="col-xs-4">
                      <%= link_to searched_path(profile) do%>
                      <img src="<%=profile.image %>">
                      <% end %>
                    </div><!-- end row -->

                    <div class="col-xs-4">
                      <%= link_to searched_path(profile) do%>
                      <%=profile.firstname%>
                      <% end %>

                      <%= link_to searched_path(profile) do%>
                      <%=profile.lastname%>
                      <% end %>

                      <%= link_to searched_path(profile) do%>
                      <%=profile.city%>
                      <% end %>

                      <%= link_to searched_path(profile) do%>
                      <%=profile.role%>
                      <% end %>
                    </div><!-- end row -->


                    <div class="col-xs-4">
                      <%= link_to "Add Connection", friendships_path(:friend_id => @profile.user), :method => :post, class:"btn btn-primary " %>
                      <br>
                      <%= link_to "View Profile", searched_path(profile), class:"btn btn-danger " %>
                    </div>

                    </div><!-- end row -->
                    <%end%>
                  </div>







              <% elsif current_user.profile.seeker == 'sourcer' %>
              <h1> HEYYYYYYYY </h1>
                <!-- <% for profile in @profiles.where(seeker: "candidate") %>
                  <tr>
                    <td>
                      <%= link_to searched_path(profile) do%>
                      <img src="<%=profile.image %>">
                      <% end %>
                    </td>
                    <td>
                      <%= link_to searched_path(profile) do%>
                      <%=profile.firstname%>
                      <% end %>
                    </td>
                    <td>
                      <%= link_to searched_path(profile) do%>
                      <%=profile.lastname%>
                      <% end %>
                    </td>
                    <td>
                      <%= link_to searched_path(profile) do%>
                      <%=profile.city%>
                      <% end %>
                    </td>
                    <td>
                      <%= link_to searched_path(profile) do%>
                      <%=profile.role%>
                      <% end %>
                    </td>
                  </tr >

                <%end%> -->
              <% end %>








            </div><!-- end table thumbnail -->
            <%= will_paginate @profiles %>
      </div>  <!-- end col-xs-8 -->

    </div>


  </div>
</section>
